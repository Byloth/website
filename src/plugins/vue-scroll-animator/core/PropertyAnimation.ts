interface PropertyOptions
{
    property: string;

    startValue: number;
    endValue?: number;
    // unit?: string; -> px / em / rem

    // timing?: string; -> linear / ease / cubic
}

export { PropertyOptions };

//
// TODO: Creare, anche qui, una classe per compilare i metodi di calcolo.
//
